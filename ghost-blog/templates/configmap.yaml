apiVersion: v1
kind: ConfigMap
metadata:
  name: ghost-config
  namespace: ghost-blog
data:
  database__client: "mysql"
  database__connection__host: "ghost-mysql"  # ✅ Match MySQL service name
  database__connection__user: "ghost"
  database__connection__password: "ghostpassword"
  database__connection__database: "ghostdb"  # ✅ Match the database name in MySQL
  url: "https://blog.cloudmasa.com"
